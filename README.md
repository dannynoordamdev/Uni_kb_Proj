This project can be initialized by:

cd Uni_kb_Proj/Client
~ Client npm install --force
~ Client npm run dev

cd Uni_kb_Proj/Server
~ Server dotnet run

----
After dotnet run (backend, asp.net core) and npm run dev (client, react), vite (server) will output a local host adress on which this project can be inspected.
